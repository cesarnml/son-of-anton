**Local-quality notes**

- **Duplication:** Checked `parseSubagentRunnerOptions` against `parseReviewPolicy`, the two `{model?, effort?}` types, effort-platform rules, and non-blank string checks. Found a parallel unknown-key loop (~5 lines, same shape as `parseReviewPolicy`), identical `SubagentRunnerOptionEntry` / `ResolvedRunnerOptions` shapes, cursor-cli effort disallowed in both `config.ts` and `resolveRunnerOptions`, and an inline `{ model?, effort? }` in `cli-runner.ts` instead of reusing `ResolvedRunnerOptions`. `parseSubagentRunnerOptions` also reimplements non-blank string checks that `optionalNonBlankString` already covers.
- **Naming:** Checked new identifiers and nearby call-site types. `resolvedRunnerOptionsForAttempt` matches the existing `runnerSelfReport` closure-mutation pattern; the comment clarifies it holds the last iteration’s values. The `usedRunner` annotation on line 1207 still omits `'cursor-cli'` even though `fallbackOutcome.ranKind` can be — stale local type, not introduced by P21.04 logic itself.
- **Dead code:** Checked exports and new symbols. `RUNNERS_SUPPORTING_EFFORT` and `VALID_CLAUDE_EFFORT_TIERS` are exported from `config.ts` but only referenced inside that file (no test or cross-module consumers).
- **Complexity:** Checked `parseSubagentRunnerOptions`, `resolveRunnerOptions`, and the `subagent-review` attempt closure. Scope is proportional to the ticket; the closure-mutation carry-out mirrors pre-existing `runnerSelfReport` mutation in the same callback rather than adding a new pattern. `parseSubagentRunnerOptions` is ~87 lines but linear; no obvious over-branching beyond what validation requires.
- **Test-name/behavior alignment:** Checked all three P21.04 describe blocks. Names match unit-level assertions. One gap: `"leaves runnerModel/runnerEffort absent for a default run"` exercises `buildRunnerInvocation` with options omitted (keys absent/`undefined`), while `cli-runner.ts` always passes `resolvedRunnerOptionsForAttempt.model ?? null` / `.effort ?? null` (explicit `null` keys on the ledger row) — production path is untested and uses different serialization than the test name implies. No tests cover `--subagent-model` / `--subagent-effort` CLI parsing in `cli.ts`. `"records runnerModel/runnerEffort for an overridden run"` covers builder/validator only, not resolver → spawn → ledger integration.

**Suggestions**

<refactor-suggestions>
- (tools/delivery/cli-runner.ts subagent-review attempt closure) — Import and use `ResolvedRunnerOptions` for `resolvedRunnerOptionsForAttempt` instead of duplicating the inline `{ model?: string; effort?: string }` shape.
- (tools/delivery/config.ts + tools/delivery/subagent-runner.ts runner option types) — Collapse `SubagentRunnerOptionEntry` and `ResolvedRunnerOptions` into one shared `{ model?: string; effort?: string }` type (config imports from runner module or a small shared types export) to avoid two names for the same shape.
- (tools/delivery/config.ts RUNNERS_SUPPORTING_EFFORT / VALID_CLAUDE_EFFORT_TIERS) — Drop the `export` keyword if nothing outside `config.ts` consumes them, or relocate effort-capability constants beside `resolveRunnerOptions` in `subagent-runner.ts` so config validation and runtime resolution share one source for “which platforms accept effort.”
- (tools/delivery/config.ts parseSubagentRunnerOptions) — Reuse `optionalNonBlankString` (with a small wrapper for platform-scoped error labels) for `model`/`effort` non-blank checks instead of inline `typeof` + `trim()` blocks.
- (tools/delivery/test/subagent-runner.test.ts P21.04 ledger fidelity block) — Add a case asserting explicit `null` `runnerModel`/`runnerEffort` round-trip (matching the `?? null` call site), or rename the “absent” test to `"omits runnerModel/runnerEffort when buildRunnerInvocation options are omitted"` so it does not read like an end-to-end default-run assertion.
- (tools/delivery/cli-runner.ts usedRunner) — Widen the annotation to include `'cursor-cli'` (or drop the explicit union and let `fallbackOutcome.ranKind` infer) so the local type matches `ProgrammaticSubagentRunner | 'skipped'`.
</refactor-suggestions>

**Runner termination**

`runnerStatus`: completed

`terminatedReason`: Read all touched implementation and test files and reported local-quality signals only, per the refactor-leg template.
