**Invariant results**

- **Invariant 1 — held:** `shouldFallbackToOtherRunner` returns `true` only for `unavailable`, `timeout`, and `ran` with `runner_failed` / `rate_limit` / `sandbox_denied`; it returns `false` for `ran` + `completed` and `ran` + `advisory_violation`. The three-way OR is explicit with no inverted logic or missing enum members.
- **Invariant 2 — held:** `buildSubagentFallbackOrder` yields each programmatic runner once; the loop pushes each kind once per iteration with no re-entry or duplicate push.
- **Invariant 3 — held:** `run` + `completed` hits the early-return branch (`!shouldFallbackToOtherRunner`), so the loop stops immediately and downstream `decideAdvisoryRunnerOutcome` / `hasHonestFindings` behavior for completed attempts is unchanged.

**Surface results**

- **Surface 1 (predicate exclusions) — probed:** Read `shouldFallbackToOtherRunner` (~754–768). `completed` and `advisory_violation` are excluded by the explicit three-reason allowlist; no typo or incomplete list widens the trigger set to `completed`.
- **Surface 2 (loop early-return vs break) — probed:** Traced the `if (ran && !shouldFallback)` / `if (!shouldFallback) break` chain. Fallback-eligible `ran` results skip the early return and skip `break`, so the loop continues. Non-fallback `ran` returns; non-`ran` non-fallback statuses break correctly.
- **Surface 3 (write tracking across attempts) — probed:** Traced `runProgrammaticSubagentReview` and the adversarial path in `cli-runner.ts`. **Regression found:** when attempt 1 is `ran` + `runner_failed` with file writes (`tryRunner` → `outcome: 'patched'`), P21.03 now falls back. `runProgrammaticSubagentReview` is saved by `wholeInvocationWrite` (~1487–1503), but `cli-runner.ts` (~1139–1184) overwrites outer `outcome`/`terminatedReason` per attempt with no whole-invocation write guard, so a successful fallback can record `clean`/`completed` despite an earlier write violation.
- **Surface 4 (`fallbackFrom` semantics) — probed:** Verified `fallbackFrom: index === 0 ? null : requested` (~578). For a middle-runner failure followed by success, `fallbackFrom` stays the originally requested runner, not the failed intermediate one. No dedicated test for middle-runner failure; logic is correct by inspection.
- **Surface 5 (downstream outcome deciders) — probed:** `decideAdvisoryRunnerOutcome` still handles non-`completed` `terminatedReason` on the terminal `failed_all` path. Successful fallback paths only return `ran` + `completed`. `decideSubagentOutcomeFromRunner` branches for non-completed are effectively test-only legacy; not a blocking gap for this ticket.
- **Diff class 1 (schema / `VALID_TERMINATED_REASONS`) — probed:** No new `terminatedReason` values or ledger fields. Fallback paths reuse existing reasons (`runner_failed`, `rate_limit`, `sandbox_denied`, `completed`, `runner_unavailable`). Legacy validators remain compatible.
- **Diff class 2 (CLI flags) — N/A:** No new CLI flags, help text, or validators in this diff.
- **Diff class 3 (`catch` error breadth) — probed:** `tryRunner`'s `catch` is unchanged (spawn exception → `unavailable`). Broadened fallback does not add catches or blur spawn exceptions vs classified `ran` failures.
- **Diff class 4 (defensive layering / malformed `ran`) — probed:** `shouldFallbackToOtherRunner` is called only from the fallback loop (and a log branch in `cli-runner.ts`). A malformed `ran` with `terminatedReason: undefined` would not match the allowlist → no fallback → early return; it would not fall through to `break`. TypeScript types require `terminatedReason`, but runtime behavior is safe.
- **Diff class 5 (cross-file atomicity) — N/A:** Pure in-memory loop/predicate change; no new multi-step filesystem writes.
- **Diff class 6 (test-contract strength) — probed:** P21.03 tests assert `ranKind`, `fallbackFrom`, `fallbackLevel`, `attemptedKinds`, and call order for success and `failed_all` paths. Gap: no test for write + `runner_failed` fallback interaction on the production `cli-runner` path; the `advisory_violation` no-advance test uses a synthetic `terminatedReason: 'advisory_violation'` that `tryRunner` never emits (production path is `patched`/`completed` + write detection).
- **Diff class 7 (doc vs code / Rationale) — probed:** Ticket Rationale and orchestrator fallback paragraph match the predicate/loop diff. Doc drift: orchestrator (~88) says the full attempt chain lives in `attemptedKinds`, but `SubagentRunnerInvocation` has no `attemptedKinds` field and `cli-runner.ts` writes a single ledger row per invocation — `attemptedKinds` exists only on the in-memory `runSubagentWithFallback` return value (pre-existing, not introduced by P21.03).

<actionable-findings>

- tools/delivery/subagent-runner.ts, tools/delivery/cli-runner.ts — P21.03 broadens fallback for all `ran` + `{runner_failed, rate_limit, sandbox_denied}` without excluding `outcome: 'patched'` (write-detected attempts). When the first runner writes files and also classifies as `runner_failed`, the loop now advances; in the adversarial `subagent-review` path (`cli-runner.ts` ~1041–1184), per-attempt `decideAdvisoryRunnerOutcome` overwrites outer `outcome`/`terminatedReason`, and there is no `wholeInvocationWrite` guard (unlike `runProgrammaticSubagentReview`), so a successful fallback can silently record `outcome: 'clean'` / `terminatedReason: 'completed'` instead of `advisory_violation`. This breaks the ticket outcome that write violations must not advance the chain and is a demonstrable correctness gap (finding-discipline clause 2). **Fix:** In `shouldFallbackToOtherRunner`, return `false` for `ran` attempts with `outcome === 'patched'` (write signal from `tryRunner`) before checking `terminatedReason`; add a regression test mirroring `p20-02.test.ts`'s write-detection case but with first-attempt `runner_failed` + fallback on the `cli-runner` integration path (or route adversarial review through `runProgrammaticSubagentReview` and add the same whole-invocation write check there).

</actionable-findings>

<advisory-observations>

- The P21.03 `advisory_violation` no-advance test uses `ranWith('advisory_violation')`, which never occurs in production (`tryRunner` emits `completed`/`runner_failed`/etc.; `advisory_violation` is decided post-loop from writes). The meaningful production guard is write detection + fallback predicate interaction, which is untested.
- `p11-04.test.ts` covers `rate_limit` and `sandbox_denied` on the predicate but not `runner_failed` directly (covered indirectly via P21.03 integration tests).
- No test exercises middle-runner failure (e.g. requested `cursor-cli`, `claude-cli` fails `runner_failed`, `codex-cli` succeeds) for `fallbackFrom`; code inspection says it would be correct (`requested`, not the failed middle runner).
- `runProgrammaticSubagentReview` (refactor-review path) still invokes a fallback runner when attempt 1 writes + `runner_failed`, even though `wholeInvocationWrite` restores the final `advisory_violation` outcome — extra cost and dirty-worktree exposure, aligned with ticket wording but not ideal.
- Attack-surface prompt lists `runProgrammaticSubagentReview` as the sole production call site of `runSubagentWithFallback`; the adversarial `subagent-review` handler in `cli-runner.ts` (~1041) also calls it directly with different post-loop write semantics.
- `docs/template/delivery/delivery-orchestrator.md` (~88) references `attemptedKinds` on ledger rows, but the ledger schema stores one invocation per run with no `attemptedKinds` array — auditability of per-attempt failures relies on logs/events, not persisted ledger fields.
- `decideSubagentOutcomeFromRunner` non-`completed` branches are effectively dead for production fallback paths after P21.03; retained for deprecated/test compatibility only.

</advisory-observations>

<runner-termination>
runnerStatus: completed
terminatedReason: Finished probing all stated invariants and attack surfaces against the implementation, tests, and docs.
</runner-termination>
