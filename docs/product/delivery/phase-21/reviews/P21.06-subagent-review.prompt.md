You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

P21.06 Multi-issue Origin close

`parseOriginIssueNumbers` (`tools/delivery/planning.ts`) returns every `Origin issue: #<N>`
line from the `## Epic` section, in document order, deduplicated; single-line plans parse
identically to today. Delivery state (`tools/delivery/types.ts`) carries the full list
(`originIssueNumbers`); existing single-issue state files load without migration. `open-pr`
for the phase's final ticket only appends one `- Closes #<N>` bullet per origin issue to
the PR body; earlier tickets' PRs get none (unchanged semantics — issues resolve only when
the last stacked PR lands on `closeoutBranch`). Near-miss lines (`Origin Issue #76`,
`origin issue: 76`, trailing punctuation) are still rejected — the parser stays strict; the
preflight format check (`.agents/skills/preflight/SKILL.md`) is updated to validate every
`Origin issue` line in the multi-line form and still FAIL on near-misses.
`docs/template/stubs/product-plan.template.md`,
`docs/template/stubs/implementation-plan.template.md`, and
`docs/template/delivery/delivery-orchestrator.md` document the multi-line form.

Review Focus (from the ticket doc, verbatim):
- State back-compat: an in-flight phase recorded with the old single `originIssueNumber`
  must keep working after upgrade — check `state.json` load paths, not just the parser.
- The final-ticket-only rule survives: grep every consumer of the origin-issue value to
  confirm none starts emitting `Closes` on intermediate PRs.
- Preflight symmetry: the SKILL.md check and the parser must accept/reject exactly the
  same forms — a form preflight passes but the parser ignores recreates the silent-skip
  bug this mechanism just fixed.
- This phase's own final PR (P21.07) is the first consumer — it must close #78, #83, #84,
  #87, #105.
- Intentionally deferred: comma-separated single-line form (`Origin issue: #78, #83`) —
  one canonical form, one line per issue.

Rationale (from the ticket doc): renamed `parseOriginIssueNumber` to
`parseOriginIssueNumbers` outright rather than keeping a wrapper. `normalizeDeliveryStateFromPersisted`
gained `coerceOriginIssueNumbersFromPersisted` (renamed from an earlier, refactor-review-
flagged name) which wraps a persisted single `originIssueNumber` into a one-element array
at load time — new field wins when both present. A refactor-review pass (cursor-cli,
advisory) found and the primary agent accepted two suggestions: dropped a third
whole-document fallback in `parseOriginIssueNumbers` (was scope creep beyond the documented
`## Epic`-scoped contract — a phase markdown with no `## Epic` heading now correctly
returns `[]` rather than scanning the entire document for stray `Origin issue:` lines),
and renamed the legacy-coercion helper for clarity.

### Files touched

Implementation:
tools/delivery/planning.ts
tools/delivery/state.ts
tools/delivery/types.ts
tools/delivery/pr-metadata.ts

Tests:
tools/delivery/test/planning.test.ts
tools/delivery/test/pr-metadata.test.ts

Docs:
docs/template/stubs/product-plan.template.md
docs/template/stubs/implementation-plan.template.md
docs/template/delivery/delivery-orchestrator.md
.agents/skills/preflight/SKILL.md

### Invariants to hold

1. `parseOriginIssueNumbers` returns exactly the set of `Origin issue: #<N>` lines that
   appear verbatim (line contains nothing but `Origin issue: #<N>`, optional trailing
   whitespace) inside the `## Epic` section — never lines outside that section, never a
   near-miss form, and the returned array is deduplicated and in document order.
2. A `state.json` persisted before P21.06 (with a scalar `originIssueNumber: <N>` field
   and no `originIssueNumbers` array) loads into a `DeliveryState` whose
   `originIssueNumbers` is `[<N>]`, without the operator running any migration command.
3. `buildPullRequestBody` emits a `- Closes #<N>` bullet for every entry in
   `state.originIssueNumbers` if and only if the ticket passed in is the last element of
   `state.tickets` — never for any other ticket, and never zero bullets when the array is
   non-empty on the final ticket.

### Attack surfaces to probe

1. `tools/delivery/planning.ts` `parseOriginIssueNumbers`'s Epic-section extraction
   (`markdown.match(/## Epic\s+([\s\S]*?)\n## /)?.[1] ?? markdown.match(/## Epic\s+([\s\S]*)$/)?.[1] ?? ''`):
   does the non-greedy `[\s\S]*?` correctly stop at the *next* `## ` heading (not the
   first occurrence of the literal string `## ` anywhere, e.g. inside a code fence or a
   nested `### ` heading that happens to contain `## ` as a substring)? Construct a plan
   markdown with a fenced code block between `## Epic` and the next real heading that
   contains a line looking like `## fake heading` and confirm origin-issue lines inside
   the fence (if any) are still captured or correctly excluded per the documented
   contract.
2. `tools/delivery/state.ts` `coerceOriginIssueNumbersFromPersisted`: for a state file
   that somehow has *both* a legacy `originIssueNumber` scalar AND a new
   `originIssueNumbers` array (e.g. hand-edited, or written by a future buggy version),
   confirm the array always wins deterministically, and that this doesn't silently drop
   the scalar value in a way that loses information the operator expected.
3. `syncStateWithPlan`'s `originIssueNumbers: originIssueNumbers ?? existing?.originIssueNumbers`
   (~line 400): on a `sync`/`start`/`advance` re-run against an *existing* state.json where
   the plan markdown's `## Epic` section has since been edited to remove all `Origin issue`
   lines (parser now returns `[]`), does the `?? existing?.originIssueNumbers` fallback
   incorrectly *keep* the stale previously-recorded issue numbers instead of clearing them
   — i.e., is `[]` (parsed-but-empty) indistinguishable from `undefined` (not parsed) at
   this call site, given `loadPlanContext` returns `undefined` (not `[]`) when the parsed
   list is empty?
4. `tools/delivery/pr-metadata.ts` `buildPullRequestBody`'s `isFinalTicketInPhase` check
   (`state.tickets[state.tickets.length - 1]?.id === ticket.id`): grep every other call
   site of `state.originIssueNumbers` (or the old `originIssueNumber`) across the
   `tools/delivery/` tree to confirm no other PR-body-building or notification path
   independently re-derives "is this the final ticket" and could emit `Closes` bullets on
   an intermediate ticket's PR through a different code path than this one.
5. Preflight/parser symmetry: read `.agents/skills/preflight/SKILL.md`'s updated Origin
   issue check text side-by-side with `parseOriginIssueNumbers`'s regex
   (`/^Origin issue:\s*#(\d+)\s*$/gm`) and confirm they accept/reject the *exact* same set
   of forms — specifically, does the SKILL.md prose's "nothing else on the line" wording
   correctly forbid the same trailing-whitespace-only tolerance the regex allows
   (`\s*$` permits trailing spaces/tabs but not other trailing characters)?
6. This phase's own final ticket (P21.07): does `implementation-plan.md`'s `## Epic`
   section (which already lists five `Origin issue:` lines per the earlier scaffold quoted
   in the ticket's own Rationale) parse to exactly `[78, 83, 84, 87, 105]` under the new
   parser, matching the Review Focus's explicit expectation?

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — `DeliveryState` gains
   `originIssueNumbers` and loses `originIssueNumber` as a *type* (though
   `coerceOriginIssueNumbersFromPersisted` still reads the old field name at runtime from
   `unknown`-typed persisted JSON). Does `JSON.stringify(state)` (via `saveState`) ever
   re-serialize a lingering `originIssueNumber` key from an old file that was merely
   spread through (`{...root, originIssueNumbers, tickets}` in
   `normalizeDeliveryStateFromPersisted`), producing a state.json with *both* keys
   persisted going forward, or does the old key get dropped on the next save?
2. **CLI flag/arg symmetry** — N/A, no new CLI flags in this ticket; confirm and record.
3. **Error-class breadth in `catch` blocks** — N/A, no new try/catch introduced by this
   diff; confirm and record.
4. **Defensive layering at module boundaries** — `coerceOriginIssueNumbersFromPersisted`
   trusts `root.originIssueNumbers` is `number[]` once `Array.isArray` passes, without
   checking element types. Could a corrupted/hand-edited state.json with
   `originIssueNumbers: ["not-a-number"]` pass through silently and later produce
   `- Closes #not-a-number` in a PR body?
5. **Cross-file atomicity windows** — N/A, no new multi-step write; confirm and record.
6. **Test-contract strength** — do the P21.06 tests assert the full array shape
   (`toEqual([78, 83, 84, 87, 105])`) rather than just length or membership? Is there a
   test exercising `normalizeDeliveryStateFromPersisted`/`coerceOriginIssueNumbersFromPersisted`
   directly against a legacy-shaped persisted object (not just `buildPullRequestBody`
   constructed with the new field), i.e. is invariant 2 actually covered by a test, or
   only asserted in the ticket doc?
7. **Doc-vs-code drift in the ticket Rationale** — read the ticket's `## Rationale` and
   the four doc files listed in Files touched against the actual
   `parseOriginIssueNumbers`/`coerceOriginIssueNumbersFromPersisted`/`buildPullRequestBody`
   diff — does any doc claim behavior (e.g. about the whole-document fallback that was
   just removed, or about migration) that the code does not actually implement?

### Diff context

```diff
--- a/tools/delivery/planning.ts
+++ b/tools/delivery/planning.ts
@@ -export function parseOriginIssueNumber(markdown): number | undefined { ... }
+export function parseOriginIssueNumbers(markdown: string): number[] {
+  const epicSection =
+    markdown.match(/## Epic\s+([\s\S]*?)\n## /)?.[1] ??
+    markdown.match(/## Epic\s+([\s\S]*)$/)?.[1] ??
+    '';  // no whole-document fallback (post-refactor-review)
+  const matches = [...epicSection.matchAll(/^Origin issue:\s*#(\d+)\s*$/gm)]
+    .map((m) => Number(m[1]));
+  return [...new Set(matches)];
+}

--- a/tools/delivery/types.ts
+++ b/tools/delivery/types.ts
@@ DeliveryState @@
-  originIssueNumber?: number;
+  originIssueNumbers?: number[];

--- a/tools/delivery/state.ts
+++ b/tools/delivery/state.ts
@@ new coercion helper, called from normalizeDeliveryStateFromPersisted @@
+function coerceOriginIssueNumbersFromPersisted(root) {
+  if (Array.isArray(root.originIssueNumbers)) return { originIssueNumbers: root.originIssueNumbers };
+  if (typeof root.originIssueNumber === 'number') return { originIssueNumbers: [root.originIssueNumber] };
+  return { originIssueNumbers: undefined };
+}
@@ syncStateWithPlan @@
-  originIssueNumber: originIssueNumber ?? existing?.originIssueNumber,
+  originIssueNumbers: originIssueNumbers ?? existing?.originIssueNumbers,
@@ loadPlanContext @@
-  const originIssueNumber = parseOriginIssueNumber(planMarkdown);
+  const originIssueNumbers = parseOriginIssueNumbers(planMarkdown);
+  // returns { originIssueNumbers: originIssueNumbers.length > 0 ? originIssueNumbers : undefined }

--- a/tools/delivery/pr-metadata.ts
+++ b/tools/delivery/pr-metadata.ts
@@ buildPullRequestBody final-ticket branch @@
-  if (state.originIssueNumber !== undefined && isFinalTicketInPhase) {
-    lines.push(`- Closes #${state.originIssueNumber}`);
-  }
+  if (isFinalTicketInPhase) {
+    for (const issueNumber of state.originIssueNumbers ?? []) {
+      lines.push(`- Closes #${issueNumber}`);
+    }
+  }
```

---

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).
`Invariant results` and `Surface results` stay free-form prose. The other
three regions — `<actionable-findings>`, `<advisory-observations>`,
`<runner-termination>` — are **copy-me tag skeletons**: copy them verbatim,
keep the tag names and closing tags exactly as shown, and put your content
between the tags. A parser (`tools/delivery/reconciliation.ts`,
`tools/delivery/subagent-runner.ts`) reads only what is between the tags —
nothing outside them, no heading recognition. If a region has nothing to
report, the block contains the single literal line `None` (no bullets).

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings** — copy this skeleton verbatim:

```
<actionable-findings>

- file/path — what is wrong, which invariant or finding-discipline clause
  applies, and a concrete fix recommendation.

</actionable-findings>
```

If none, the block contains only the literal word `None`:

```
<actionable-findings>
None
</actionable-findings>
```

**Advisory Observations** — copy this skeleton verbatim:

```
<advisory-observations>

- one observation per bullet, including any doc-vs-code drift surfaced
  under the diff-derived "Doc-vs-code drift in the ticket Rationale" class.

</advisory-observations>
```

If none, the block contains only the literal word `None`:

```
<advisory-observations>
None
</advisory-observations>
```

**Runner termination** — copy this skeleton verbatim:

```
<runner-termination>
runnerStatus: completed
terminatedReason: one short sentence explaining why this status was reported.
</runner-termination>
```

`runnerStatus` is one of `completed | rate_limit | sandbox_denied |
runner_unavailable`. `completed` means you **finished the review** per this
template — it says nothing about what you found. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` or
`outcome: completed_with_findings` for any non-`completed` `terminatedReason`
(both collapse to `skipped` with the original reason preserved), so do not
claim `completed` if you stopped early.
