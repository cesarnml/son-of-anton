**Local-quality notes**

- **Duplication:** Checked the three `terminatedReason` literals in `shouldFallbackToOtherRunner`, the paired `shouldFallbackToOtherRunner` calls in the fallback loop, and the `ran()` / `ranWith()` helpers in `subagent-runner.test.ts`. The loop double-call is pre-existing; the reason list is a single three-way OR with no shared constant (acceptable at this size). The two test helpers overlap — `ranWith('completed')` duplicates what `ran()` already builds — so there is minor test-helper duplication worth noting.
- **Naming:** Checked describe-block titles, file-header comments, and JSDoc against post-P21.03 behavior. The P11.04 describe title (`auto-fallback predicate is narrowed to binary-availability failures`) and the file-header invariant #3 (`does NOT trigger auto-fallback` for rate-limit-like output) still describe the old, narrower contract. `runSubagentWithFallback`'s JSDoc also still names only `unavailable`/`timeout` as fallback triggers. Identifier names (`shouldFallbackToOtherRunner`, `ranWith`) still fit their roles.
- **Dead code:** Checked new branches in `shouldFallbackToOtherRunner` and the loop early-return gate. Clean — no unreachable paths or leftover scaffolding from the Green pass.
- **Complexity:** Checked whether broadening the single predicate or splitting loop logic added unnecessary structure. Clean — one guard change in the loop and a straightforward `status === 'ran'` branch in the predicate; no extra abstraction beyond what the ticket needed.
- **Test-name/behavior alignment:** Checked P21.03 `it` titles against assertions and the flipped P11.04 blocks. P21.03 integration tests align well. The P11.04 describe title and file-header invariant #3 are misaligned with the flipped `it` blocks (behavior now expects fallback on `ran + rate_limit` / `sandbox_denied`). The last P21.03 test (`a genuinely completed ran result still returns immediately`) largely restates the first P14.02 `runSubagentWithFallback` success case, though with an explicit `terminatedReason`.

**Suggestions**

<refactor-suggestions>
- (tools/delivery/subagent-runner.ts runSubagentWithFallback JSDoc) — Extend the fallback-trigger bullet to include P21.03 `ran` + `{runner_failed, rate_limit, sandbox_denied}` (or a one-line “see shouldFallbackToOtherRunner”), so the function doc matches current behavior and does not contradict the predicate’s JSDoc.
- (tools/delivery/test/p11-04.test.ts describe block ~line 111) — Rename the describe title (and optionally refresh file-header invariant #3) to reflect the broadened predicate contract; the flipped `it` bodies are correct but the surrounding labels still read as the old “binary-availability only” story.
- (tools/delivery/test/subagent-runner.test.ts P14.02/P21.03 describe helpers) — Merge `ran()` and `ranWith()` into one helper with an optional `terminatedReason` (default `'completed'`) so P21.03 tests reuse the P14.02 factory instead of maintaining two near-identical builders.
</refactor-suggestions>

**Runner termination**

`runnerStatus`: `completed`

`terminatedReason`: Finished the Refactor-leg cold read of all five local-quality signals across the touched implementation, test, and doc files.
