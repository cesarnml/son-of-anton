**Invariant results**

1. **[broken]** — `parseOriginIssueNumbers` is Epic-scoped and strict for near-misses, but the `\n## ` boundary in `planning.ts` can terminate the Epic slice inside a fenced code block; valid `Origin issue:` lines after that fence are dropped (Invariant 1).
2. **[held]** — `coerceOriginIssueNumbersFromPersisted` wraps legacy `originIssueNumber` into `[N]` and prefers `originIssueNumbers` when both exist; no dedicated test (Invariant 2 **untested**).
3. **[broken]** — `buildPullRequestBody` emits one `- Closes #<N>` per entry on the final ticket only, but stale `originIssueNumbers` can persist when Epic lines are removed because `loadPlanContext` maps `[]` → `undefined` and `syncStateWithPlan` falls back with `??` (Invariant 3).

**Surface results**

1. **[probed]** — Epic extraction uses `/## Epic\s+([\s\S]*?)\n## /`. A fenced block containing `\n## fake heading` terminates the slice early; lines after the fence (e.g. `Origin issue: #83`) are excluded. `###` headings do not trigger it. Prose like `See ## Product contract` does not match `\n## `.
2. **[probed]** — When both fields exist, `Array.isArray(originIssueNumbers)` wins; scalar is ignored per spec. `{ ...root, originIssueNumbers }` can leave `originIssueNumber` in memory until `syncStateWithPlan` rebuilds; saves drop the legacy key.
3. **[probed]** — `loadPlanContext` returns `undefined` when the parser yields `[]`; `syncStateWithPlan`’s `originIssueNumbers ?? existing?.originIssueNumbers` keeps stale numbers after Epic lines are removed. Non-empty re-parses update correctly.
4. **[probed]** — Grep of `tools/delivery/` shows `originIssueNumbers` only in `state.ts`, `pr-metadata.ts`, `types.ts`, and tests. `- Closes #` appears only in `pr-metadata.ts` / tests. `open-pr` uses `buildPullRequestBody` via `ticket-flow.ts`.
5. **[probed]** — Preflight and parser align on near-miss rejection. Parser allows `\s*` after the colon and trailing `\s*`; preflight prose says “single space” and “nothing else on the line,” so it is stricter on spacing — blocks at preflight, not a silent-skip path.
6. **[probed]** — Phase-21 `implementation-plan.md` has five strict lines; static analysis yields `[78, 83, 84, 87, 105]`.
7. **[N/A — no new CLI flags]** — Confirmed.
8. **[N/A — no new try/catch]** — Confirmed.
9. **[probed]** — `coerceOriginIssueNumbersFromPersisted` casts with `as number[]` without element validation; `["not-a-number"]` would stringify into PR bullets (hand-edited state only).
10. **[N/A — no new multi-step write]** — Confirmed.
11. **[probed]** — Tests assert full array shape for five-line parse and all five `Closes` bullets; no direct test for legacy `originIssueNumber` coercion.
12. **[probed]** — Touched template/orchestrator docs match implementation. Phase-21 `implementation-plan.md` still describes pre-P21.06 single-line `parseOriginIssueNumber` behavior (advisory drift, not in the touched doc set).

```
<actionable-findings>

- tools/delivery/state.ts — `loadPlanContext` converts a parsed-empty list to `undefined` (`originIssueNumbers.length > 0 ? originIssueNumbers : undefined`), and `syncStateWithPlan` uses `originIssueNumbers ?? existing?.originIssueNumbers`. Removing all `Origin issue:` lines from `## Epic` therefore never clears persisted numbers; the final ticket's PR can still close stale issues. Correctness gap (Invariant 3). Fix: distinguish “parsed empty” from “not parsed” (e.g. pass `[]` through, or use a sentinel / explicit clear when the parser returns `[]` on re-sync).

- tools/delivery/planning.ts — Epic-section extraction stops at the first `\n## ` anywhere in the slice, including inside fenced code blocks. A plan with a code example containing a `## …` line truncates the Epic section and drops valid `Origin issue:` lines after the fence. Breaks Invariant 1. Fix: fence-aware Epic boundary detection (or heading scan that ignores fenced regions), consistent with markdown structure.

</actionable-findings>
```

```
<advisory-observations>

- Invariant 2 (legacy `originIssueNumber` → `[N]` at load) is implemented in `coerceOriginIssueNumbersFromPersisted` but has no unit test; only `pr-metadata.test.ts` exercises the new field shape on manually constructed state.

- `coerceOriginIssueNumbersFromPersisted` accepts any `Array.isArray` value without numeric element checks; corrupted `originIssueNumbers: ["not-a-number"]` would produce `- Closes #not-a-number` in the final PR body.

- When both `originIssueNumbers: []` and `originIssueNumber: <N>` exist in persisted JSON, the empty array wins per “new field wins,” silently dropping the scalar.

- Preflight SKILL.md requires “single space” after the colon; the parser allows `\s*` (tabs/multiple spaces). Preflight is stricter — unlikely silent skip, but not character-level symmetric.

- `docs/product/delivery/phase-21/implementation-plan.md` blockquote still says `parseOriginIssueNumber` honors only the first line until P21.06 — stale relative to landed behavior; P21.07 depends on all five lines parsing.

- `normalizeDeliveryStateFromPersisted` spreads the raw root (may retain `originIssueNumber` in memory) before `syncStateWithPlan` rebuilds; persisted saves drop the legacy key on the normal sync path.

</advisory-observations>
```

```
<runner-termination>
runnerStatus: completed
terminatedReason: Review finished via static analysis of all listed attack surfaces and invariants; shell execution was unavailable for runtime probes.
</runner-termination>
```
