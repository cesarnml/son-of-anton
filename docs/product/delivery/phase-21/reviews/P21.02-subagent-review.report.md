**Invariant results**

1. **[broken]** — When `cli-runner.ts` supplies a cross-check parse (`reportTextForCrossCheck` is defined), `decideAdvisoryRunnerOutcome` still returns `outcome: 'clean'` whenever `hasHonestFindings` is false. That includes an **unclosed** `<actionable-findings>` block whose body contains bullet findings: `parseActionableFindings` extracts them (`findings.length > 0`) but `hasHonestFindings` requires `closed === true`, so the row is recorded `clean` despite parsed findings. The well-formed closed-tag case is fixed; this malformed-tag case is not.

2. **[held]** — `completed_with_findings` is only emitted when `terminatedReason === 'completed'` (non-`completed` reasons return earlier) and `hasHonestFindings` is true, which requires a closed tag, non-empty bullet findings, and `!isExplicitNone`. Parser invariants prevent `isExplicitNone` coexisting with non-empty `findings`.

3. **[held]** — In `decideAdvisoryRunnerOutcome`, `runnerWroteFiles` returns `{ outcome: 'skipped', terminatedReason: 'advisory_violation' }` before the non-`completed` check, which runs before the findings cross-check. A runner that wrote files and reported findings cannot be misrecorded as `completed_with_findings`.

**Surface results**

1. **[probed]** — `hasHonestFindings` correctly rejects `found=true, closed=false` and `isExplicitNone=true` for the honest-findings predicate. The gap is the fallback: those rejections map to `clean` instead of a non-clean outcome when a cross-check parse was supplied.

2. **[probed]** — `runnerWroteFiles` short-circuits at lines 826–828, before termination and findings checks. Ordering is correct; no test covers the combined write-violation + findings path.

3. **[probed]** — When `result.stdout` and `result.rawOutput` are both `undefined`, `actionableFindingsCrossCheck` stays `undefined` and outcome falls back to `clean` without throwing. On the live `cli-runner` spawn path, `stdout` is always at least `''`, so cross-check is always supplied; empty output is classified `runner_failed` by the per-runner coercers, yielding `skipped`, not `clean`.

4. **[probed]** — v1 rows without `schemaVersion` and v2 rows with `completed_with_findings` pass `validateRunnerArtifact` (covered in tests). Unknown outcome strings fail validation (`null`). Unknown `schemaVersion: 999` round-trips per the forward-compat comment in `validateInvocation` (not test-covered, but code path is clear).

5. **[probed]** — `decideSubagentOutcomeFromRunner` has no live call site in `cli-runner.ts` (grep is empty). Recorder dispatch uses operator-supplied `clean|patched` directly via `buildRunnerInvocation('operator-recorder', ...)`. The deprecated function cannot leak `completed_with_findings` into production.

6. **[probed]** — Doc edits in `adversarial-review-template.md`, `subagent-review-report-template.md`, and `delivery-orchestrator.md` consistently separate `runnerStatus: completed` (runner finished per template) from ledger `outcome` (CLI-derived from `<actionable-findings>`). Wording attributes cross-check enforcement to `decideAdvisoryRunnerOutcome`, not to the subagent.

7. **[probed — Output stability across schema-version drift]** — v1 and v2 acceptance is tested; v999 tolerance is documented in validator comments and follows the same permissive `schemaVersion` check (integer only, no max).

8. **[N/A — CLI flag/arg symmetry]** — No new CLI flags in this diff.

9. **[N/A — Error-class breadth in catch blocks]** — No new catch blocks; existing paths do not swallow the new outcome value.

10. **[probed — Defensive layering at module boundaries]** — `subagent-runner.ts` uses a type-only import from `reconciliation.ts`. `reconciliation.ts` does not import from `subagent-runner.ts`; no circular runtime import.

11. **[N/A — Cross-file atomicity windows]** — No new multi-step persisted write sequence beyond existing append-only ledger behavior.

12. **[probed — Test-contract strength]** — Tests assert `outcome` and `terminatedReason` structurally and cover both `decideAdvisoryRunnerOutcome` and `decideSubagentOutcomeFromRunner` with and without `actionableFindings`. Gaps: no test for unclosed-tag-with-bullets → outcome, no test for `runnerWroteFiles` + findings interaction, no test for unknown-outcome rejection or `schemaVersion: 999`.

13. **[probed — Doc-vs-code drift in ticket Rationale]** — Rationale claim that refactor-review's `runProgrammaticSubagentReview` call site is untouched is accurate: `runProgrammaticSubagentReview` calls `decideAdvisoryRunnerOutcome` without `actionableFindings` (lines 1497–1499). P21.05 notification and reconcile-gate rule changes are not implemented in the diff.

```
<actionable-findings>
- tools/delivery/subagent-runner.ts — Invariant 1 / correctness gap: when the caller supplies an `actionableFindings` parse and the tag is unclosed but contains bullet findings, `parseActionableFindings` returns `findings.length > 0` with `closed: false`, `hasHonestFindings` returns false, and `decideAdvisoryRunnerOutcome` falls through to `outcome: 'clean'`. That re-creates the pre-P21.02 honesty failure for a plausible template-violation report (missing close tag, bullets present). `reconcile-subagent-review` may still block via `isSuspiciousActionableFindingsParse`, but the ledger row falsely claims `clean`. Fix: treat parsed non-empty findings as honest even when `closed` is false (or emit a distinct non-`clean` outcome for suspicious-but-findings-bearing parses), so bullet findings never coexist with `outcome: 'clean'`.
</actionable-findings>
```

```
<advisory-observations>
- tools/delivery/subagent-runner.ts + tools/delivery/cli-runner.ts — When cross-check is supplied but the parse is suspicious without extractable bullets (missing tag, closed-empty-without-`None`, prose-only body), outcome is still `clean` while `isSuspiciousActionableFindingsParse` fires a record-time warning. Invariant 1's stricter reading ("clean only on explicit `None` or omitted cross-check") is not fully enforced at the ledger layer; `reconcileReview` in `reconciliation.ts` (lines 348–361) treats suspicious parses as blocking findings, partially backstopping open-pr.
- tools/delivery/cli-runner.ts — `buildRunnerInvocation` for programmatic `subagent-review` does not populate the ledger `findings: string[]` field from `actionableFindingsCrossCheck` when outcome is `completed_with_findings`; findings remain recoverable only from the report sidecar. Ticket Review Focus flagged this for P21.05; not a P21.02 blocker but worth noting for notification work.
- tools/delivery/test/p21-02.test.ts — No regression test for `runnerWroteFiles: true` combined with honest findings (ordering is correct in code but untested), and no test for the unclosed-tag-with-bullets misclassification path above.
- docs/product/delivery/phase-21/ticket-02-outcome-honesty-completed-with-findings.md — Outcome bullet "A `clean` row now guarantees an empty findings block" is stronger than what the code guarantees for suspicious/malformed parses that still record `clean`; reconcile gate compensates at open-pr time but not at ledger-record time.
</advisory-observations>
```

```
<runner-termination>
runnerStatus: completed
terminatedReason: Reviewed all listed attack surfaces and invariants via static analysis of the diff and directly related implementation paths; shell verification was unavailable in this environment.
</runner-termination>
```
