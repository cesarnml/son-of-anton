You are conducting a Refactor-leg cold read of a code change that has already passed
Red and Green (failing test written, then made to pass). Your job is local code-quality
signal on the diff — NOT correctness, NOT invariants, NOT attack surfaces. A separate
adversarial gate covers those; do not duplicate it.

### Ticket scope

P21.04 Per-platform model and effort selection with ledger fidelity

`orchestrator.config.json` accepts an optional `subagentRunnerOptions` per-platform map:
`{ "claude-cli": { "model": string?, "effort": string? }, "codex-cli": { "model": string?,
"effort": string? }, "cursor-cli": { "model": string? } }`; unknown platforms or option
keys fail config validation with actionable errors. `subagent-review` accepts flat
`--subagent-model <value>` and `--subagent-effort <value>` flags that apply only to the
explicitly requested runner (via `--subagent` or configured `subagentRunner`); fallback
runners resolve from `subagentRunnerOptions` alone. Precedence per platform: flag
(requested runner only) > config entry > platform default. `buildRunnerSpawnCommand`
forwards resolved values into each platform's documented flags: `claude --model <m>
--effort <e>`, `codex exec -m <m> -c model_reasoning_effort=<e>`, `agent --model <m>`
(cursor-cli has no effort flag). Values a platform cannot express fail fast before spawn.
`SubagentRunnerInvocation` ledger rows gain optional `runnerModel`/`runnerEffort`.

Rationale (from the ticket doc): one resolver (`resolveRunnerOptions`) returning
`{model?, effort?}` per attempt, consumed uniformly by the P21.03 fallback loop via the
same `attempt(runner)` closure that already exists at the `subagent-review` call site —
chosen over threading model/effort through `RunnerAttemptResult`/
`shouldFallbackToOtherRunner` because resolution depends on the *requested* runner and
repo config, neither of which the fallback-loop core has access to. Intentionally
deferred: the `subagent-refactor-review` gate (`runProgrammaticSubagentReview`) still
calls `buildRunnerSpawnCommand` without model/effort options — the ticket text scopes the
flat flags to `subagent-review` specifically.

### Files touched

Implementation:
tools/delivery/config.ts
tools/delivery/subagent-runner.ts
tools/delivery/cli.ts
tools/delivery/cli-runner.ts

Tests:
tools/delivery/test/subagent-runner.test.ts
tools/delivery/test/runtime-config.test.ts

### Local-quality context

`tools/delivery/config.ts`: adds `SubagentRunnerOptionEntry`/`SubagentRunnerOptions`
types, `RUNNERS_SUPPORTING_EFFORT`/`VALID_CLAUDE_EFFORT_TIERS` constants, and
`parseSubagentRunnerOptions` (validates platform keys, entry keys, non-blank model,
effort-not-supported-on-cursor-cli, and claude-cli effort tier membership). Wired into
`loadOrchestratorConfig`/`resolveOrchestratorConfig`/`OrchestratorConfig`/
`ResolvedOrchestratorConfig`.

`tools/delivery/subagent-runner.ts`: adds `ResolvedRunnerOptions` type and
`resolveRunnerOptions({runner, requestedRunner, flagModel, flagEffort, configOptions})`
— flag applies only when `runner === requestedRunner`; throws when a resolved `effort`
lands on `cursor-cli`. Extends `buildRunnerSpawnCommand`'s options with `model`/`effort`
and forwards them per platform. Extends `SubagentRunnerInvocation`,
`BuildRunnerInvocationOptions`, `buildRunnerInvocation`, and `validateInvocation` with
`runnerModel`/`runnerEffort` (optional, nullable, string-typed, permissive round-trip
matching the existing `fallbackFrom`/`primaryAgent` field pattern).

`tools/delivery/cli.ts`: adds `--subagent-model`/`--subagent-effort` flag parsing
(same shape as the existing `--commit`/`--reason` flag blocks) and threads them onto
`ParsedCliArgs`.

`tools/delivery/cli-runner.ts`: in the `subagent-review` command's fallback-attempt
closure (the `attempt(runner)` callback passed to `runSubagentWithFallback`), calls
`resolveRunnerOptions` per attempt, stores the result in a closure-scoped
`resolvedRunnerOptionsForAttempt` variable (mirrors the existing `runnerSelfReport`
mutation pattern in the same closure), passes `model`/`effort` into
`buildRunnerSpawnCommand`, and reads `resolvedRunnerOptionsForAttempt` when building the
final `buildRunnerInvocation` call to populate `runnerModel`/`runnerEffort`.

Areas to give a cold read: whether `resolvedRunnerOptionsForAttempt`'s closure-mutation
pattern is the cleanest way to carry per-attempt state out of the fallback loop, or
whether it should be threaded through `runSubagentWithFallback`'s return value instead;
whether `parseSubagentRunnerOptions` in `config.ts` (roughly 70 lines) duplicates
validation shape already present in `parseReviewPolicy`'s unknown-key-rejection loop
closely enough to warrant a shared helper; whether the `RUNNERS_SUPPORTING_EFFORT`/
`VALID_CLAUDE_EFFORT_TIERS` constants belong in `config.ts` or `subagent-runner.ts`
given `resolveRunnerOptions` (which also encodes the cursor-cli-has-no-effort rule) lives
in the latter; test-name/behavior alignment across the new P21.04 describe blocks in
`subagent-runner.test.ts` and `runtime-config.test.ts`.

---

### Your directives

**Scope — local quality signals only:**
- Duplication: repeated logic that could be extracted or reused.
- Naming: identifiers that no longer match what they do after Green.
- Dead code: unreachable branches, unused exports, leftover scaffolding.
- Complexity: functions/branches that grew past what the ticket needed.
- Test-name/behavior alignment: test names that no longer describe what they assert.

Do NOT report: correctness bugs, missing invariants, attack surfaces, security issues, or
architecture opinions beyond the diff at hand. Those belong to the adversarial gate.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**No fabrication pressure:** If the diff is already clean on all five signals, your
correct output is a clean report (literal `None` in the tag block below). Do not invent
suggestions to justify the review step.

---

### Required output format

Report in this exact structure (prose only — no file edits).

**Local-quality notes**
For each of the five signals (duplication, naming, dead code, complexity, test-name/
behavior alignment): one line noting what you checked and what you found, or "clean."

**Suggestions**
Copy this tag block verbatim and fill it in. One bullet per suggestion, each naming the
touched file and function and the specific change. If there is nothing to suggest, the
block contains only the literal word `None` — no bullets, nothing else.

<refactor-suggestions>
- (touched-file.ts function-or-area) — specific suggested change
- (touched-file.ts function-or-area) — specific suggested change
</refactor-suggestions>

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
