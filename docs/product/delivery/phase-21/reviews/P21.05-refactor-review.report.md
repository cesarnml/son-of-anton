**Local-quality notes**

- **Duplication:** Checked the two `emitNotificationWarnings`/`eventsForSubagentReviewCommand` call sites in `cli-runner.ts` (operator-recorder vs programmatic-runner branches) and the mirrored builders in `notifications.ts`; the only repeated skeleton is the shared four-argument emit pattern, while outcome/options construction legitimately diverges per the ticket Rationale — clean.
- **Naming:** Checked new union member (`subagent_review_recorded`), builders (`buildSubagentReviewRecordedEvent`, `eventsForSubagentReviewCommand`), and imports (`SubagentRunnerOutcome`); all follow existing `review_recorded` / `eventsForRecordReviewCommand` conventions — clean.
- **Dead code:** Checked the new type fields, private builder, exported `eventsForSubagentReviewCommand`, `formatNotificationMessage` fallback branch (`recorded as ${event.outcome}` for `patched`/`deferred`), and `buildNotificationPayload` routing; nothing unused or unreachable within the diff — clean.
- **Complexity:** Checked `formatNotificationMessage`'s new case; the four-level nested ternary for `summary` is denser than sibling cases' inline `.filter(Boolean).join('\n')` arrays but still readable and scoped to one case — borderline, not over-built elsewhere.
- **Test-name/behavior alignment:** Checked all three `describe` blocks in `p21-05.test.ts`; two names overclaim command-handler behavior the assertions do not exercise (not-found em-dash clause; best-effort `it` claiming the command does not fail while only calling `notifyBestEffort`).

**Suggestions**

<refactor-suggestions>
- (notifications.ts formatNotificationMessage `subagent_review_recorded` case) — replace the nested-ternary `summary` with a small local switch or `formatSubagentReviewRecordedSummary(...)` helper so outcome phrasing matches the flat readability of sibling switch cases
- (p21-05.test.ts `P21.05 — eventsForSubagentReviewCommand`) — shorten the not-found `it` name to drop the em-dash clause about commands that did not record an outcome; the test only asserts `eventsForSubagentReviewCommand` returns `[]` for a missing ticket id
- (p21-05.test.ts `P21.05 — notifier failure stays best-effort`) — rename the `it` to describe `notifyBestEffort` warning surfacing (e.g. mirror orchestrator.test.ts's `keeps notification failures best-effort`) and drop "without failing the command" unless a cli-runner integration assertion is added
</refactor-suggestions>

**Runner termination**

`runnerStatus`: completed

`terminatedReason`: Read all touched implementation and test files plus sibling notification patterns; assessed the five local-quality signals and reported only advisory findings proportional to what the diff actually shows.
