**Invariant results**

1. `[held]` — Read `parseActionableFindings`, `parseAdvisoryObservations`, and their `*Result` siblings in `reconciliation.ts`. Grep and tests confirm no heading path remains: legacy `**Actionable findings**`, `## Actionable findings`, and plain-text headings with bullets all yield `found: false`, empty lists, and `isSuspicious*Parse === true`.

2. `[held]` — Closed tags with literal `None` (and `None.`) set `isExplicitNone: true`, empty item lists, and `isSuspiciousActionableFindingsParse` / `isSuspiciousAdvisoryObservationsParse` both return false, matching the record-time silence requirement.

3. `[held]` — In `cli-runner.ts`, the loud-floor block runs only after `terminatedReason` is finalized from `decideAdvisoryRunnerOutcome`, and is gated on `terminatedReason === 'completed'`. Skipped/rate-limited/advisory-violation paths skip it. Missing/unclosed/misnamed tags and empty-without-`None` bullet regions correctly accumulate in `suspiciousRegions`. One asymmetry noted under Advisory Observations: empty but closed `<runner-termination>` does not warn.

**Surface results**

**Ticket-spec-derived**

1. `reconcileReview` boolean migration — `[probed]` — Uses `parseActionableFindings(...).findings.length > 0`, not truthiness on the object. No JS-object-is-truthy bug.

2. Last-open-tag-wins — `[probed]` — `parseTaggedBulletBlock` and `parseRunnerTermination` both take `openMatches[openMatches.length - 1]`. Test fixture with quoted skeleton then real block selects the real finding. Logic is correct.

3. Coercer degradation without `<runner-termination>` — `[probed]` — All three coercers call `parseRunnerStatusTrailer` → `parseRunnerTermination`, which returns `{ found: false, runnerStatus: null }` without throwing. They fall through to rate-limit checks, then exit-code/empty-stdout heuristics. No silent throw; no unhandled exception path found.

4. Loud-floor vs `terminatedReason` ordering — `[probed]` — `terminatedReason` is assigned at lines 1139–1140; loud floor runs at 1203–1236 using that value. Skipped outcomes do not spuriously warn; completed outcomes with malformed reports do warn.

5. `parseAdvisoryObservations` vs `parseAdvisoryObservationsResult` — `[probed]` — Wrapper is `return parseAdvisoryObservationsResult(markdown).observations`; single extraction path, no divergence or residual normalization.

6. Deleted heading machinery — `[probed]` — Repo-wide grep shows no surviving TS call sites for `extractReportSection`, `stripHorizontalRules`, `isHeadingFor`, or `CANONICAL_REPORT_SECTION_HEADINGS`. Only comments/docs reference them.

7. `state.ts` refactor-review allowlist — `[probed]` — All eight fields (`refactorReviewOutcome`, `refactorReviewCompletedAt`, `refactorReviewPatchCommits`, `refactorReviewAgent`, `refactorRunnerArtifactPath`, `refactorReviewPromptPath`, `refactorReviewPromptWrittenAt`, `refactorReviewedHeadSha`) are present with correct names matching `types.ts`. Test asserts four of eight; code path is complete.

**Diff-derived classes**

1. Output stability across schema-version drift — `[probed]` — `parseActionableFindings` return type changed from `boolean` to structured object; only two TS call sites (`reconcileReview`, `cli-runner` loud floor), both updated. Ledger JSON schema unchanged. Mid-phase mixed-contract risk is documented in `delivery-orchestrator.md` consumer upgrade rule. p16-03/p16-04 fixtures migrated to tagged reports.

2. CLI flag/arg symmetry — `[N/A — reason: no new or changed CLI flags in this diff.]`

3. Error-class breadth in `catch` blocks — `[N/A — reason: diff adds no new catch blocks.]`

4. Defensive layering at module boundaries — `[probed]` — Parsers accept raw markdown strings with no revalidation beyond tag extraction; consistent with `parseRefactorSuggestions` and intentional for this layer. `reconcileReview` trusts caller-supplied `reportMarkdown` as before.

5. Cross-file atomicity windows — `[probed]` — Loud floor runs after `writeSubagentReviewOutcome` but before `appendInvocationToArtifact` / commit. Warning reflects parsed stdout even if subsequent commit fails — acceptable, not a contract violation.

6. Test-contract strength — `[probed]` — Parser tests assert structural shape (`found`, `closed`, `isExplicitNone`, items) before content. No integration test asserts the `cli-runner.ts` loud-floor `console.log` or its `terminatedReason === 'completed'` gate; parser unit tests cover the predicates it uses.

7. Doc-vs-code drift in ticket Rationale — `[probed]` — Rationale matches implementation: structured actionable result, advisory wrapper kept, loud floor gated on `completed`, no shared helper, state fix separate. Drift items in Advisory Observations below.

<actionable-findings>

- tools/delivery/reconciliation.ts — `reconcileReview` Condition B checks only `.findings.length > 0`, so non-bullet prose inside a properly closed `<actionable-findings>` tag (e.g. a paragraph without `- ` prefix) yields zero parsed findings, triggers the record-time loud floor via `isSuspiciousActionableFindingsParse`, but does **not** block `open-pr`. Pre-P21.01 `parseActionableFindings` returned `true` for any non-`None` section body including prose (old template allowed "single paragraph or bullet"). **Clause 2 correctness gap:** semantic actionable content can exist in the persisted report while reconciliation returns `{ kind: 'clean' }` if the primary agent ignores the one-time console warning. **Fix:** at `reconcileReview` (or `reconcile-subagent-review`), treat `isSuspiciousActionableFindingsParse(parseActionableFindings(reportMarkdown))` as blocking Condition B (or equivalent), or extract prose lines as findings when the tag is closed and non-empty.

</actionable-findings>

<advisory-observations>

- Doc-vs-code drift: `notes/public/subagent-report-parser-contract.md` header (lines 8–9) still lists `extractReportSection` as an owner of `parseAdvisoryObservations`; that machinery was deleted in this diff. Contract body describes the tag model correctly.

- Doc-vs-code drift: `docs/template/delivery/adversarial-review-template.md` has stray standalone ` ``` ` fences at lines 260 and 284 (after the runner-termination skeleton and at EOF). They are outside the copy-me tag examples but may confuse agents filling the prompt.

- `isSuspiciousRunnerTerminationParse` only tests `!found || !closed`, not an empty closed body missing `runnerStatus:`. A `<runner-termination></runner-termination>` block is not flagged at record time; coercers still classify `terminatedReason: 'completed'` on exit 0 + non-empty stdout. Asymmetric with actionable/advisory empty-without-`None` handling.

- Ticket Outcome says "Triage-time surfacing remains as backstop." `advisory-observation-warnings.ts` still only flags missing/empty report files (`inspectSubagentReviewEvidence`) and untriaged successfully parsed observations — not tag-parse health (`isSuspicious*Parse`). Malformed-tag reports that produce zero observations are silently skipped at triage (`observations.length === 0` → continue). Record-time loud floor is the sole tag-health signal; triage does not backstop missed record-time warnings.

- `orchestrator.test.ts` regression test verifies four of eight refactor-review state fields; the other four (`refactorReviewCompletedAt`, `refactorReviewPatchCommits`, `refactorReviewAgent`, `refactorRunnerArtifactPath`) are in code but untested — low risk given copy-paste pattern.

- Rationale claims "Downstream consumers … behave identically for the clean/none case" (Review Focus). True for tagged `None`. Legacy heading-format `None.` reports now parse as missing-tag suspicious at record time and zero findings at reconciliation — intentional per phase decision, but not identical for in-flight legacy reports on disk.

</advisory-observations>

<runner-termination>
runnerStatus: completed
terminatedReason: review finished against the filled prompt; all ticket-spec attack surfaces probed, one Condition B fail-open path found for prose-in-tag actionable content.
</runner-termination>
