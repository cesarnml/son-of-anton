**Local-quality notes**

- **Duplication:** Checked `hasHonestFindings` against `isSuspiciousActionableFindingsParse` in `reconciliation.ts` and the two `parseActionableFindings` call sites in `cli-runner.ts`'s programmatic `subagent-review` branch. The predicates are complementary but split across modules; the same report text is parsed twice when `terminatedReason === 'completed'`.
- **Naming:** Checked new identifiers (`hasHonestFindings`, `reportTextForCrossCheck`, `completed_with_findings`) against existing reconciliation/parser naming. `hasHonestFindings` is ticket-clear but diverges from the `ActionableFindings*` vocabulary in `reconciliation.ts`; otherwise clean.
- **Dead code:** Checked all `decideSubagentOutcomeFromRunner` call sites. The new `actionableFindings` parameter and `hasHonestFindings` branch are never passed from production or tests — only the deprecated, test-retained function carries them.
- **Complexity:** Checked `decideAdvisoryRunnerOutcome`, `hasHonestFindings`, and the `cli-runner.ts` cross-check wiring. Branching matches ticket scope; no unnecessary growth beyond the optional-parse gate.
- **Test-name/behavior alignment:** Checked `p21-02.test.ts` titles against assertions. Names match behavior for the advisory path and ledger validation; no test covers the deprecated `decideSubagentOutcomeFromRunner` path the ticket/docs describe as parallel.

**Suggestions**

<refactor-suggestions>
- `tools/delivery/cli-runner.ts` (programmatic `subagent-review` `result.status === 'ran'` block): hoist a single `parseActionableFindings(reportTextForCrossCheck)` result and pass it to both `decideAdvisoryRunnerOutcome` and the later `isSuspiciousActionableFindingsParse` loud-floor check instead of parsing the same text twice.
- `tools/delivery/reconciliation.ts` / `tools/delivery/subagent-runner.ts`: add an exported positive counterpart to `isSuspiciousActionableFindingsParse` (e.g. `hasActionableFindingsContent`) colocated with the suspicious helper, and replace private `hasHonestFindings` with that shared predicate.
- `tools/delivery/subagent-runner.ts` `decideSubagentOutcomeFromRunner`: either add a `p21-02.test.ts` case exercising `actionableFindings` on the deprecated path or remove the unused param/branch until a real caller exists — it is currently unreachable scaffolding.
</refactor-suggestions>

**Runner termination**

`runnerStatus`: `completed`

`terminatedReason`: Finished the refactor-leg cold read across the implementation, tests, reconciliation helpers, and call-site wiring per the five local-quality signals.
