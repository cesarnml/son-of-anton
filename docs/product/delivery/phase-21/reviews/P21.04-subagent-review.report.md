**Invariant results**

1. **[held]** Traced `resolveRunnerOptions` precedence and the P21.04 unit tests (`flag beats config`, fallback ignores flags). `model` and `effort` resolve independently via `isRequested` ternaries; fallback attempts read only `configOptions[runner]`. CLI/config boundaries reject blank strings, so production paths do not feed `''` into the resolver.

2. **[held]** Confirmed `RUNNERS_SUPPORTING_EFFORT` excludes `cursor-cli`. `resolveRunnerOptions` throws before `tryRunner` for cursor effort from flags or config; `parseSubagentRunnerOptions` rejects `cursor-cli` effort at config load. Tests cover both paths.

3. **[held]** In `cli-runner.ts`, `resolvedRunnerOptionsForAttempt` is reassigned at the start of each `attempt(runner)` callback, before `tryRunner`. On success, `runSubagentWithFallback` returns from the winning iteration; on `failed_all`, the loop completes all attempts and the closure retains the last runner’s resolved options. Ledger write uses `resolvedRunnerOptionsForAttempt.model ?? null` / `.effort ?? null` with `usedRunner` from `fallbackOutcome.ranKind`.

**Surface results**

1. **[probed]** Attack surface 1 (`resolveRunnerOptions` partial override / `??` vs `''`): Independent fields use separate ternaries; partial flag override correctly falls through to config for the other field on the requested runner only. `''` would be treated as a defined value in the resolver (not `undefined`), but `parseCliArgs` and `optionalNonBlankString` block blank values at ingress; `buildRunnerSpawnCommand` uses truthy checks so a hypothetical `''` would not spawn flags — defense-in-depth gap only, not reachable from validated production paths.

2. **[probed]** Attack surface 2 (`resolveRunnerOptions` throw vs `tryRunner` catch): `resolveRunnerOptions` sits outside the `tryRunner` spawn closure; throws propagate through `attempt()` → `runSubagentWithFallback` → `cli-runner` top-level `catch` (line ~2109), which logs and returns exit 1. Not swallowed by `tryRunner`’s spawn-only `catch`.

3. **[probed]** Attack surface 3 (`failed_all` stale `resolvedRunnerOptionsForAttempt`): Each loop iteration overwrites the closure variable before spawn. Early success returns from the winning iteration; `failed_all` exhausts the order and leaves the last attempted runner’s options. No stale earlier-attempt retention under P21.03 fallback behavior.

4. **[probed]** Attack surface 4 (`cursor-cli` + `effort: ""`): `parseSubagentRunnerOptions` checks platform support before `optionalNonBlankString`; `{ "cursor-cli": { "effort": "" } }` yields a single “not supported” error, not a double-error path.

5. **[probed]** Attack surface 5 (circular import / absent `subagentRunnerOptions`): `config.ts` does not import `subagent-runner.ts`; one-way import only. `resolveOrchestratorConfig` passes through `subagentRunnerOptions` as `undefined` when absent; `baseConfig` fixtures without the field work with `resolveRunnerOptions({ configOptions: undefined })`.

6. **[probed]** Diff-derived 1 (schema-version drift): `SUBAGENT_LEDGER_SCHEMA_VERSION` remains `2`; `validateInvocation` treats `runnerModel`/`runnerEffort` as optional typed fields. No exhaustive key-iteration consumer found; forward-compatible additive fields.

7. **[probed]** Diff-derived 2 (CLI flag/arg symmetry): `--subagent-model`/`--subagent-effort` are parsed globally in `cli.ts` onto `ParsedCliArgs` but consumed only in the `subagent-review` invoke-runner branch. Other commands accept and silently ignore them.

8. **[probed]** Diff-derived 3 (error-class breadth): `resolveRunnerOptions` cursor-effort errors are plain `Error` objects; they reach the same top-level handler as spawn failures. No intermediate catch swallows them.

9. **[probed]** Diff-derived 4 (unvalidated config to resolver): Production `resolveRunnerOptions` call site is `cli-runner.ts` subagent-review only, using `context.config.subagentRunnerOptions` from `loadOrchestratorConfig` → `parseSubagentRunnerOptions`. Direct test calls supply validated shapes.

10. **[probed]** Diff-derived 5 (cross-file atomicity): N/A — ledger append path unchanged except two optional fields on the same atomic write.

11. **[probed]** Diff-derived 6 (test-contract strength): Tests assert `resolveRunnerOptions` return shapes and `buildRunnerSpawnCommand` args arrays directly; config validation covered in `runtime-config.test.ts`. Gaps: no `parseCliArgs` tests for the new flags; no single end-to-end chain test (config load → resolve → spawn args → ledger row) through `cli-runner`.

12. **[probed]** Diff-derived 7 (doc-vs-code drift): See Advisory Observations — minor drift in ticket Outcome schema example and `delivery-orchestrator.md` flag table vs implementation/README.

```
<actionable-findings>
None
</actionable-findings>
```

```
<advisory-observations>

- `docs/template/delivery/delivery-orchestrator.md` (flag table, ~line 499) claims `--subagent-effort` is validated against known claude-cli tiers, but `cli.ts` accepts any non-blank string; tier validation runs only in `parseSubagentRunnerOptions` for config entries. `README.md` correctly documents both flags as free-form — internal doc inconsistency.

- `docs/product/delivery/phase-21/ticket-04-per-platform-model-effort-selection.md` Outcome bullet (~line 10) shows `codex-cli` with only `model`, omitting `effort`; implementation, tests, and `delivery-orchestrator.md` all support `codex-cli` effort via `-c model_reasoning_effort=<e>`.

- Global CLI parsing means `--subagent-model`/`--subagent-effort` on non-`subagent-review` commands (e.g. `open-pr`, `status`) parse successfully but are silently ignored — operators may believe overrides applied when they did not.

- `resolveRunnerOptions` does not re-validate non-blank strings or claude effort tiers for flag-sourced values; it relies on `parseCliArgs`/`parseSubagentRunnerOptions` at boundaries. Direct unit-test calls with `flagModel: ''` would return `{ model: '' }` while `buildRunnerSpawnCommand` would omit spawn flags (truthy guard), creating a resolver/spawn/ledger mismatch — not reachable from validated CLI/config paths.

- Test coverage gaps (also noted in the refactor-review report): no `parseCliArgs` tests for `--subagent-model`/`--subagent-effort`; no integration test exercising the full `subagent-review` closure (resolver → spawn → ledger) including fallback `failed_all` last-runner fidelity.

- Intentionally deferred per ticket Rationale: `runProgrammaticSubagentReview` / `subagent-refactor-review` still spawns without model/effort despite `subagentRunnerOptions` in config — documented, not a regression.

</advisory-observations>
```

```
<runner-termination>
runnerStatus: completed
terminatedReason: All ticket-spec invariants and attack surfaces were probed; no blocking correctness gaps found under the finding-discipline clauses.
</runner-termination>
```
