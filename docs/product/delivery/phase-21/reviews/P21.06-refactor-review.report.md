**Local-quality notes**

- **Duplication:** Checked Epic-section extraction vs `parsePlan` section regexes, the three `syncState*` pass-throughs, and legacy normalization vs ticket legacy helpers — no repeated logic worth extracting; `normalizeLegacyOriginIssueNumbers` is called once and mirrors existing helper style in `state.ts`. Clean.
- **Naming:** Checked renames (`parseOriginIssueNumbers`, `originIssueNumbers`, param threading) — plural names match array semantics; only mild mismatch is `normalizeLegacyOriginIssueNumbers`, which reads the new array field first and only then the legacy scalar.
- **Dead code:** Checked for lingering `parseOriginIssueNumber` / single-field consumers — old export and callers are gone; no unreachable branches in the touched paths. Clean.
- **Complexity:** Checked `parseOriginIssueNumbers`'s three-step `??` Epic fallback and `normalizeLegacyOriginIssueNumbers`'s two-branch precedence — helper is proportionate and consistent with neighboring normalization; the whole-document third fallback is the one extra branch (untested, wider than the Epic-scoped docstring).
- **Test-name/behavior alignment:** Checked `planning.test.ts` and `pr-metadata.test.ts` describe/it titles against assertions — titles match behavior; fixtures with trailing `## Product contract` match real `implementation-plan.md` layout and exercise the bounded-section regex; `'rejects near-miss formats'` means silent non-match (empty array), which fits prior convention; `'returns an empty list when no origin issue line is present'` omits the trailing heading (covers the Epic-to-EOF branch, not a naming mismatch).

**Suggestions**

<refactor-suggestions>
- (planning.ts parseOriginIssueNumbers) — Drop the third `?? markdown` fallback (or return `[]` when `## Epic` is absent) so extraction stays Epic-scoped as documented; add a test if a no-`## Epic` case must stay supported.
- (state.ts normalizeLegacyOriginIssueNumbers) — Rename to something like `coerceOriginIssueNumbersFromPersisted` (or inline the three-branch ternary in `normalizeDeliveryStateFromPersisted`) so the name reflects new-field-first coercion, not legacy-only handling.
</refactor-suggestions>

**Runner termination**

`runnerStatus`: completed  
`terminatedReason`: Finished cold read of all touched implementation and test files against the five local-quality signals.
