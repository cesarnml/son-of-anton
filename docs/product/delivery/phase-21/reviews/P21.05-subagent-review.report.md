**Invariant results**

1. **[broken]** — The operator-recorder and programmatic-runner paths each emit exactly one event after recording. The doc-only auto-skip path (`cli-runner.ts` ~841–854) calls `recordSubagentReview`, persists state, and returns with **no** `emitNotificationWarnings`, so a command that records `skipped` can emit zero events. The two ledger-writing branches behave correctly; fallback resolves to a single post-loop emit.

2. **[held]** — On both recording branches, notification reads `nextState` and the same outcome variable used for the ledger row (`dispatch.outcome` or post-fallback `outcome`). The programmatic branch correctly notifies ledger-honest `completed_with_findings` while state stores mapped `clean`; `terminatedReason` and `findingsCount` come from the same post-decision variables as `buildRunnerInvocation`.

3. **[held]** — `formatSubagentReviewRecordedSummary` has explicit cases for all three ticket-specified outcomes. `completed_with_findings` never uses “passed” language; `skipped` includes `terminatedReason` when the programmatic caller supplies it (always initialized, spread when `outcome === 'skipped'`).

**Surface results**

1. **[probed]** — Operator-recorder branch (~950–958): `emitNotificationWarnings` runs after `saveState`/`formatStatus`, passes `nextState`, `subagentTarget.id`, and `dispatch.outcome` with no extra options. Correct for `clean`/`patched` recorder outcomes.

2. **[probed]** — Programmatic branch (~1346–1362): spread-conditionals only attach `terminatedReason` for `skipped` and `findingsCount` for `completed_with_findings`. `patched`/`deferred` are unreachable from the programmatic advisory path (`decideAdvisoryRunnerOutcome` never returns them); operator-recorder `patched` hits the `default` branch (`recorded as patched`), which is acceptable and not success-shaped.

3. **[probed]** — `runSubagentWithFallback` returns once; `outcome`/`terminatedReason`/`actionableFindingsCrossCheck` are assigned only inside the attempt callback on `status === 'ran'` and reflect the final resolved attempt. A single `emitNotificationWarnings` follows the loop — no per-attempt notifications.

4. **[probed]** — `eventsForSubagentReviewCommand` re-looks up the ticket from `nextState` by id; `recordSubagentReview` does not mutate `title`/`branch`. Same `subagentTarget.id` and outcome variable are used for ledger write and notification — no title/branch vs outcome mismatch.

5. **[probed]** — `formatSubagentReviewRecordedSummary` explicitly handles `clean`, `skipped`, and `completed_with_findings`. `default` only fires for operator-recorder `patched` in production; it cannot incorrectly subsume the three primary outcomes.

6. **[probed]** — Only consumer switch on `DeliveryNotificationEvent.kind` is `formatNotificationMessage`, updated with `case 'subagent_review_recorded'`. TypeScript exhaustiveness holds; `emitReviewCleanGate` filters `review_recorded` only — no break.

7. **[N/A — no new CLI flags in this ticket]** — Confirmed; emission only.

8. **[probed]** — Both new call sites use bare `await emitNotificationWarnings(...)` with no additional try/catch; failures surface via existing `notifyBestEffort` warning path.

9. **[probed]** — `eventsForSubagentReviewCommand` trusts caller-supplied `outcome`/options without re-reading the ledger. Current call sites pass the same values just written; no present lying-notification path. Future misuse remains possible (advisory).

10. **[probed]** — Notification follows `commitDeliveryArtifactAndPush` and `saveState`. A crash between persist and emit can drop a notification; this matches the established best-effort contract (same as `review_recorded` siblings) and does not violate correctness invariants.

11. **[probed]** — Tests assert full event shape via `toEqual`/`toMatchObject` and message text, plus `notifyBestEffort` failure behavior. **No** test exercises `cli-runner.ts` `subagent-review` integration for either recording branch or the doc-only auto-skip path.

12. **[probed]** — Rationale matches code (two branch-specific emit sites, `dispatch.outcome` vs `outcome`/`stateOutcome` split). Minor drift: ticket Outcome bullet says “`buildNotificationPayload` case” but implementation adds the `formatNotificationMessage` case (which `buildNotificationPayload` already delegates to).

```
<actionable-findings>

- tools/delivery/cli-runner.ts — The doc-only auto-skip path (~841–854) records a `skipped` outcome via `recordSubagentReview` and `saveState` but returns without calling `emitNotificationWarnings`, violating invariant 1 (zero notifications for a command that records an outcome). The sibling `poll-review` doc-only auto-skip path in the same file (~1995–1999) does emit after recording, so this gap is inconsistent and leaves operators without notification on a common path. Fix: after `saveState`, call `emitNotificationWarnings(notifier, cwd, eventsForSubagentReviewCommand(nextState, subagentTarget.id, 'skipped', { terminatedReason: '<doc-only-policy-reason>' }))` (or omit `terminatedReason` only if none is honestly available), mirroring the poll-review pattern.

</actionable-findings>
```

```
<advisory-observations>

- tools/delivery/test/p21-05.test.ts — No integration test invokes the `subagent-review` command handler; coverage stops at `notifications.ts` builders/formatters plus direct `notifyBestEffort`. The doc-only auto-skip gap and both emit call sites in `cli-runner.ts` are untested end-to-end.
- docs/product/delivery/phase-21/ticket-05-subagent-review-notification.md — Outcome bullet references a “`buildNotificationPayload` case”; code adds `formatNotificationMessage`'s `subagent_review_recorded` case instead (functionally correct via existing delegation).
- docs/product/delivery/phase-21/ticket-05-subagent-review-notification.md — Red spec still lists “no event for commands that did not record a subagent-review outcome”; the committed test only covers a missing ticket id in the builder, not non-recording command paths (no-op dispatch, record-deferred subcommand).
- tools/delivery/cli-runner.ts — `subagent-review record-deferred` (~754–813) appends a ledger `deferred` row but emits no notification; out of explicit ticket scope but analogous to the doc-only gap if deferred outcomes should eventually notify.
- tools/delivery/notifications.ts — `eventsForSubagentReviewCommand` does not validate `outcome`/options against the ledger row just written; safe at current call sites but a future caller could emit a notification that does not match persisted state.
- Review Focus tension — “after the ledger row is written” can be read to exclude doc-only auto-skip (no ledger row), while invariant 1 and the Red spec speak broadly of “recorded outcome”; the doc-only path sits in that ambiguity.

</advisory-observations>
```

```
<runner-termination>
runnerStatus: completed
terminatedReason: All listed attack surfaces and invariants were probed via direct reads of implementation, tests, and ticket docs; review delivered in required template.
</runner-termination>
```
