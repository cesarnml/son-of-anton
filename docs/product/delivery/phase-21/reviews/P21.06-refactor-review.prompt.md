You are conducting a Refactor-leg cold read of a code change that has already passed
Red and Green (failing test written, then made to pass). Your job is local code-quality
signal on the diff — NOT correctness, NOT invariants, NOT attack surfaces. A separate
adversarial gate covers those; do not duplicate it.

### Ticket scope

P21.06 Multi-issue Origin close

`parseOriginIssueNumber` (`tools/delivery/planning.ts`) is superseded by a multi-line-aware
parser (`parseOriginIssueNumbers`) that returns every `Origin issue: #<N>` line from the
`## Epic` section, in document order, deduplicated; single-line plans parse identically to
today. Delivery state (`tools/delivery/types.ts`) carries the full list
(`originIssueNumbers`); existing single-issue state files load without migration. `open-pr`
for the phase's final ticket only appends one `- Closes #<N>` bullet per origin issue to
the PR body; earlier tickets' PRs get none (unchanged semantics). Near-miss lines are still
rejected — the parser stays strict. `docs/template/stubs/product-plan.template.md`,
`docs/template/stubs/implementation-plan.template.md`,
`docs/template/delivery/delivery-orchestrator.md`, and
`.agents/skills/preflight/SKILL.md` document the multi-line form.

Rationale (from the ticket doc): renamed `parseOriginIssueNumber` to
`parseOriginIssueNumbers` outright rather than keeping the old name as a wrapper — only
one caller (`state.ts`'s `loadPlanContext`), so renaming cost one call-site edit and kept
the parser's contract unambiguous. `normalizeDeliveryStateFromPersisted` gained a small
legacy-read helper that wraps a persisted single `originIssueNumber` into a one-element
array at load time. A deprecated single-value accessor (`originIssueNumbers[0]`) was
considered and rejected — only two real consumers, both cheaper to update directly.

### Files touched

Implementation:
tools/delivery/planning.ts
tools/delivery/state.ts
tools/delivery/types.ts
tools/delivery/pr-metadata.ts

Tests:
tools/delivery/test/planning.test.ts
tools/delivery/test/pr-metadata.test.ts

Docs (non-code, not in refactor scope but part of the same commit):
docs/template/stubs/product-plan.template.md
docs/template/stubs/implementation-plan.template.md
docs/template/delivery/delivery-orchestrator.md
.agents/skills/preflight/SKILL.md

### Local-quality context

`planning.ts`: `parseOriginIssueNumbers` extracts the `## Epic` section text (falling back
to end-of-string if no next `## ` heading follows, and to the whole markdown if no `## Epic`
heading is found at all), then runs `/^Origin issue:\s*#(\d+)\s*$/gm` per line, dedupes via
`Set`. Old function is fully removed (no lingering export).

`state.ts`: `LoadPlanContextResult.originIssueNumber` → `originIssueNumbers?: number[]`;
`loadPlanContext` now returns `undefined` when the parsed list is empty (was: `undefined`
when no single match). `syncStateFromScratch`/`syncStateFromExisting`/`syncStateWithPlan`
all rename their trailing optional param from `originIssueNumber?: number` to
`originIssueNumbers?: number[]`, same position, same fallback-to-existing pattern
(`originIssueNumbers ?? existing?.originIssueNumbers`). New
`normalizeLegacyOriginIssueNumbers` helper function reads either the new array field (wins)
or the old scalar field (wrapped in a one-element array) from a raw persisted object;
`normalizeDeliveryStateFromPersisted` calls it once and spreads the result into both its
early-return branch (malformed `tickets`) and its normal branch.

`types.ts`: `DeliveryState.originIssueNumber?: number` → `originIssueNumbers?: number[]`,
comment rewritten to describe the multi-issue behavior and the legacy-read normalization.

`pr-metadata.ts`: the single `if (state.originIssueNumber !== undefined && ...)` +
single `lines.push` became `if (isFinalTicketInPhase) { for (const issueNumber of
state.originIssueNumbers ?? []) { lines.push(...) } }` — the `undefined`-vs-`[]` check
collapsed into iterating an empty array (a no-op) rather than an explicit guard.

Areas to give a cold read: whether `normalizeLegacyOriginIssueNumbers`'s two-branch
precedence (new field wins, else legacy scalar, else `undefined`) is the clearest way to
express "prefer the new shape, read the old one as a fallback," or whether a simpler
inline expression would do; whether the `parseOriginIssueNumbers` Epic-section-extraction
fallback chain (three `??`-chained match attempts) is proportionate given the ticket only
asks for `## Epic`-section scoping, or whether the multi-line-plan.test.ts test fixtures
(which all now include a trailing `## Product contract` heading to give the regex a real
section boundary) read naturally against what `parsePlan`'s own section-extraction regexes
look like elsewhere in the same file; test-name/behavior alignment between the renamed
`planning.test.ts`/`pr-metadata.test.ts` describe/it blocks and what they actually assert.

---

### Your directives

**Scope — local quality signals only:**
- Duplication: repeated logic that could be extracted or reused.
- Naming: identifiers that no longer match what they do after Green.
- Dead code: unreachable branches, unused exports, leftover scaffolding.
- Complexity: functions/branches that grew past what the ticket needed.
- Test-name/behavior alignment: test names that no longer describe what they assert.

Do NOT report: correctness bugs, missing invariants, attack surfaces, security issues, or
architecture opinions beyond the diff at hand. Those belong to the adversarial gate.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**No fabrication pressure:** If the diff is already clean on all five signals, your
correct output is a clean report (literal `None` in the tag block below). Do not invent
suggestions to justify the review step.

---

### Required output format

Report in this exact structure (prose only — no file edits).

**Local-quality notes**
For each of the five signals (duplication, naming, dead code, complexity, test-name/
behavior alignment): one line noting what you checked and what you found, or "clean."

**Suggestions**
Copy this tag block verbatim and fill it in. One bullet per suggestion, each naming the
touched file and function and the specific change. If there is nothing to suggest, the
block contains only the literal word `None` — no bullets, nothing else.

<refactor-suggestions>
- (touched-file.ts function-or-area) — specific suggested change
- (touched-file.ts function-or-area) — specific suggested change
</refactor-suggestions>

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.

`completed` means you finished the review per this template. The other three values are
honest failure modes — the CLI refuses to record `outcome: clean` for any non-`completed`
`terminatedReason`, so do not claim `completed` if you stopped early.
