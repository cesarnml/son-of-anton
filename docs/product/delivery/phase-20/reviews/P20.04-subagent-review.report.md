Invariant results

1. [held] — Exercised zero-deferred and empty invocation lists; `extractDeferredRefactorReviewRows` returned `[]`, and `formatDeferredRefactorSuggestions` returned exactly `undefined`.
2. [broken] — Missing files, absent completed tickets, and syntactically invalid JSON are silent, but schema-corrupt valid JSON can throw after parsing. Examples include `{"invocations":null}`, `{"invocations":{}}`, `{"invocations":[null]}`, and deferred findings containing a non-string value.
3. [held] — Traced `advanceToNextTicket` and cook-mode `applyAdvanceBoundaryMode`. Only the reviewed ticket transitions to `done`; the newly started ticket transitions from `pending` to `in_progress`, so it cannot match the completed-ticket predicate.
4. [held] — The header-adjacent “What shipped” paragraph explicitly says only `disabled | runner_on_red` shipped and that `runner_on_red_strict` remains unimplemented future work.

Surface results

1. [probed] Ticket/path traversal — Plan ticket IDs are parsed with `[A-Z0-9.]+`, excluding `/` and traversal characters. The derived ledger path therefore cannot escape through a normally parsed ticket ID. Internally persisted `refactorRunnerArtifactPath` is also derived from that path.
2. [probed] Cook-mode transition detection — `advanceToNextTicket` changes exactly one `reviewed` ticket to `done`; cook mode only selects a ticket that was `pending` in both states before starting it. A stale previously-done ticket cannot be selected as the new ticket or newly match the completion predicate.
3. [probed] Reason fallback — The literal string `"0"` is truthy and survives `.trim() || fallback`. Empty or whitespace-only strings correctly use `(no reason recorded)`, consistent with `record-deferred` rejecting blank reasons.
4. [probed] `start-here.md` numbering — The Execute sequence now ends consistently at step 12. The only later numbered references are to steps 11 and 12, and both remain accurate.
5. [probed] Ethos conditionality — The numbered flow step and section opening both explicitly require `runner_on_red`, `Red: required`, and non-doc-only scope. The section also clearly states that disabled policy makes the entire section a no-op.
6. [probed] Design-note claims — `VALID_REFACTOR_REVIEW_VALUES` contains only `disabled` and `runner_on_red`; invalid strict mode is rejected by config validation. This repository omits the field, resolving to `disabled`, so the shipped-policy claims are accurate.
7. [probed] Output stability across schema drift — The extractor only uses `findings[0]`, so extra findings are ignored without assuming exactly one exists. However, malformed field types can throw; see the actionable finding.
8. [N/A — no CLI flags or arguments were introduced by P20.04.]
9. [probed] Catch-block breadth — Syntactic JSON/read failures are swallowed consistently with the reconciliation loaders. The new distinct failure path occurs after the catch when structurally invalid parsed data reaches the extractor.
10. [probed] Defensive module boundaries — Direct execution demonstrated `TypeError` for null/non-array `invocations`, null rows, and non-string `findings[0]`. The unchecked type assertion does not provide runtime validation.
11. [N/A — the ticket adds only ledger reads and console output, with no new cross-file writes.]
12. [probed] Test-contract strength — The six targeted tests pass and assert exact pure-function results for rows present, empty invocations, and zero deferred rows. They do not execute `advance`, simulate a missing ledger, verify completed-ticket selection, or cover corrupt/malformed ledger handling.
13. [probed] Ticket Rationale — The prompt’s placeholder description is stale. The Rationale is filled and accurately documents the pure-function approach, silence contract, deferred strict mode, and lack of behavior deviation.

Actionable findings

- `tools/delivery/cli-runner.ts`, `printDeferredRefactorSuggestionsForAdvance`, and `tools/delivery/refactor-review.ts`, `extractDeferredRefactorReviewRows`: a valid-JSON but structurally corrupt ledger crashes `advance` after it has already changed and saved delivery state. This breaks invariant 2’s corrupt-ledger tolerance and is a demonstrated correctness gap. The caller converts `artifact.invocations ?? []` through a type assertion without checking that it is an array; the extractor then assumes every row is an object and every first finding is a string. Validate the parsed artifact at runtime, treating invalid structures as no rows, or wrap parsing plus extraction/formatting in the existing silent failure boundary. Add tests for null/non-array `invocations`, null rows, and non-string findings.

Advisory Observations

- The Red contract asked for tests that `advance` itself prints rows and remains silent without a ledger. The implemented tests use empty invocations as a proxy and never exercise the CLI wrapper, filesystem behavior, state transition detection, or console output. This does not itself demonstrate another production bug, but it allowed the malformed-ledger crash and integration behavior to remain uncovered.

- No ticket Rationale drift was found. Its current text is complete and consistent with the implementation; the review prompt’s claim that it remained an unfilled placeholder is outdated.

Runner termination

`runnerStatus`: completed  
`terminatedReason`: All stated invariants and attack surfaces were inspected, with targeted tests and malformed-ledger reproductions completed.
