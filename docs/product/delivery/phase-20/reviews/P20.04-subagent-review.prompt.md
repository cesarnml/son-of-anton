You are conducting an adversarial review of a code change.
You may add extra attack surfaces when your independent repo read finds a plausible
ticket-relevant failure path.
Findings outside the three finding-discipline clauses belong in **Advisory Observations** —
anything off-scope but real is welcome there.
Your job is not a general code review — it is a targeted attack on the behavior this ticket is supposed to
protect. Start from the invariants and attack surfaces below, then independently inspect
the diff and directly related implementation code for missing ticket-relevant risks. You
are looking for paths where the ticket's intended behavior breaks, not for general
improvements.

### Ticket scope

Ticket: P20.04 Advance-time surfacing & remaining docs (final ticket of phase 20)

Outcome (from docs/product/delivery/phase-20/ticket-04-advance-surfacing-remaining-docs.md):
- `advance` reads the just-completed ticket's `reviews/<ticket>-refactor-review.ledger.json` (when present) and prints any `deferred` rows (summary + reason) to console/handoff output, so a human sees them at the next natural stopping point rather than only in a post-phase sweep.
- When no refactor-review ledger exists for the ticket, `advance` prints nothing extra — no error, no empty-section noise.
- `.agents/skills/son-of-anton-ethos/SKILL.md` updated so its execution-mechanics and stop-condition sequencing account for the new gate the same way it already accounts for the adversarial gate.
- `notes/public/refactor-advisory-subagent-design.md` header updated from `Status: PROPOSED` to `Status: IMPLEMENTED`.
- `docs/template/overview/start-here.md` / `README.md` touched only if their exact command-sequence listings need the new steps inserted for accuracy.

Review Focus (from the ticket doc):
- Confirm the "nothing extra when no ledger / no deferred rows" cases are actually silent — a chatty `advance` on every ticket regardless of policy would be worse than the phase-end-only status quo this is meant to improve on.
- Confirm the design note's `Status: IMPLEMENTED` update accurately reflects what shipped (two-state policy, no strict mode, no auto-enable) rather than restating the original proposal's full three-state ambition.
- Confirm `son-of-anton-ethos/SKILL.md`'s changes don't alter its behavior for repos running with `refactorReview: "disabled"` (the default) — the ethos skill must not assume the gate is on.

Rationale: no deviation from spec. This is the final ticket of phase 20 (subagent-refactor-review). No retrospective has been written yet — that is a separate, post-merge step per repo policy (`docs/product/retrospectives/phase-20-subagent-refactor-review-retrospective.md`), out of this ticket's scope.

### Files touched

Implementation:
tools/delivery/cli-runner.ts
tools/delivery/refactor-review.ts

Docs:
.agents/skills/son-of-anton-ethos/SKILL.md
docs/template/overview/start-here.md
notes/public/refactor-advisory-subagent-design.md

Tests:
tools/delivery/test/p20-04.test.ts

### Invariants to hold

1. `extractDeferredRefactorReviewRows` must return an empty array (not throw, not return a placeholder row) for an invocations list with zero `outcome: 'deferred'` rows, and `formatDeferredRefactorSuggestions` must return `undefined` (not an empty string) for an empty rows array — the CLI's `if (message)` check relies on `undefined`, not falsy-empty-string, to suppress output.
2. `printDeferredRefactorSuggestionsForAdvance` must not throw when the ledger file does not exist, when it exists but is corrupt/unparseable JSON, or when `completedTicket` cannot be determined (e.g. `advance` was a no-op).
3. `printDeferredRefactorSuggestionsForAdvance`'s "just completed ticket" detection (comparing `prevState`/`nextState` for a `done` transition) must identify exactly the ticket `advance` just marked `done` — not the ticket cook-mode subsequently auto-started (which would be `in_progress` in `nextState`, not `done`).
4. The design-note status-header update must not silently drop the fact that `runner_on_red_strict` remains unimplemented — a reader skimming only the header must not conclude the full three-state design shipped.

### Attack surfaces to probe

1. `printDeferredRefactorSuggestionsForAdvance` in `tools/delivery/cli-runner.ts`: it reads `completedTicket.refactorRunnerArtifactPath ?? deriveRefactorReviewLedgerPath(...)` — for a ticket where the refactor gate never ran (e.g. `refactorReview: "disabled"`), `refactorRunnerArtifactPath` is `undefined`, so it falls back to the derived path and checks `existsSync`. Confirm this path derivation can never accidentally resolve to a real, unrelated file that happens to exist (e.g. via path traversal from a malformed `ticket.id`), producing spurious output.
2. In cook mode, `advance` marks the current ticket `done` **and** immediately starts the next ticket, transitioning it to `in_progress`. Confirm the "just completed" ticket-detection logic (`before.status !== 'done' && t.status === 'done'`) does not also accidentally match the newly-started next ticket if its status representation ever overlaps (e.g. a state-sync edge case where a ticket's `before` status was already stale/`done` from a prior partial run).
3. `extractDeferredRefactorReviewRows`'s reason extraction (`row.findings?.[0]?.trim() || '(no reason recorded)'`): does the `||` fallback incorrectly treat a legitimate but falsy-string reason (e.g. the literal string `"0"`) as missing? (Probably moot given `record-deferred` requires a non-blank `--reason`, but confirm.)
4. The doc updates: does `start-here.md`'s renumbered step list (steps 7–12, inserting the new step 11) preserve correct numbering and cross-references throughout the rest of that file, or did the renumbering leave a stale reference to the old step count elsewhere in the same doc?
5. `son-of-anton-ethos/SKILL.md`'s new "Subagent Refactor Review" section: does it correctly gate every one of its instructions behind the `refactorReview: "runner_on_red"` condition stated at its top, or does any individual numbered step read as unconditionally applicable if a reader skips the opening caveat?
6. The design-note's "What shipped" addition: cross-check it against `tools/delivery/config.ts`'s actual `VALID_REFACTOR_REVIEW_VALUES` and this repo's actual `orchestrator.config.json` to confirm the claims ("two-state policy", "ships disabled everywhere", "runner_on_red_strict... rejected by config validation") are accurate as of this diff, not just as originally planned.

#### Diff-derived attack surfaces

1. **Output stability across schema-version drift** — no persisted artifact shape changed; `extractDeferredRefactorReviewRows` reads the existing `SubagentRunnerArtifact.invocations[].findings` field, already part of the schema since ticket 20.01/20.02. Confirm no assumption about `findings` always having exactly one element beyond the `?.[0]` optional access. [probed / N/A / blocked]
2. **CLI flag/arg symmetry** — no new CLI flags were added in this ticket. [probed / N/A / blocked]
3. **Error-class breadth in `catch` blocks** — `printDeferredRefactorSuggestionsForAdvance`'s `try { JSON.parse(...) } catch { return; }` swallows all parse errors identically, matching the existing convention elsewhere in this file (`loadReconciliationContext`, `loadRefactorReconciliationContext`). Confirm consistency, not a new distinct failure mode being silently absorbed. [probed / N/A / blocked]
4. **Defensive layering at module boundaries** — `extractDeferredRefactorReviewRows` and `formatDeferredRefactorSuggestions` are pure functions with no internal validation of their inputs; the CLI caller casts the parsed JSON to the expected shape with a type assertion (`as Array<{...}>`) rather than runtime validation. Confirm a malformed (but valid-JSON) ledger file cannot cause a crash rather than just producing no/garbled output. [probed / N/A / blocked]
5. **Cross-file atomicity windows** — this ticket introduces a read-only print step with no new writes; no new atomicity window. [probed / N/A / blocked]
6. **Test-contract strength** — do the new tests in `p20-04.test.ts` assert the pure functions' exact return values (not just prose), and do they cover all three Red-section scenarios (rows present, no ledger via empty-invocations proxy, zero deferred rows) distinctly? [probed / N/A / blocked]
7. **Doc-vs-code drift in the ticket Rationale** — the ticket's `## Rationale` section is still the unfilled placeholder (not yet completed for this final ticket). Confirm this is acceptable per repo policy, or flag that a phase-closing ticket might warrant more than the bare-minimum policy given the retrospective is coming next. [probed / N/A / blocked]

### Diff context

This ticket is the last of phase 20 (subagent-refactor-review). It closes the loop opened by
ticket 20.01's design decision that deferred refactor suggestions "surface again at the next
`advance`'s output" rather than being silently dropped, and updates the remaining
operator-facing docs.

Key changes:
- `tools/delivery/refactor-review.ts`: adds `extractDeferredRefactorReviewRows` (pure, filters
  ledger invocations for `outcome: 'deferred'`, extracting the reason from `findings[0]`) and
  `formatDeferredRefactorSuggestions` (pure, returns `undefined` for empty input so the caller
  can suppress output entirely).
- `tools/delivery/cli-runner.ts`: adds `printDeferredRefactorSuggestionsForAdvance`, called from
  the `advance` case right after `formatStatus` prints. It diffs `state`/`nextState` to find the
  ticket that transitioned to `done`, reads its ledger (silently returning if missing/corrupt),
  and prints formatted deferred rows if any exist.
- `.agents/skills/son-of-anton-ethos/SKILL.md`: inserts the refactor-gate step into the numbered
  per-ticket sequence (explicitly conditional on `refactorReview: "runner_on_red"`) and adds a new
  "Subagent Refactor Review" section mirroring the existing "Subagent Review" section's structure,
  with an opening caveat that the whole section is a no-op under the repo default `"disabled"`.
- `notes/public/refactor-advisory-subagent-design.md`: flips `Status: PROPOSED` to `Status:
  IMPLEMENTED`, adding a "What shipped" paragraph distinguishing the actually-shipped two-state
  policy from the original three-state proposal, and cross-referencing the product plan and
  implementation-plan docs.
- `docs/template/overview/start-here.md`: inserts a new numbered step for the refactor gate into
  the existing "Execute" sequence (which already enumerated the adversarial gate's steps at this
  granularity) and a short explanatory paragraph. `README.md` was deliberately left unchanged —
  it does not enumerate the adversarial gate's steps at the same level of detail, so adding the
  refactor gate there would be a speculative addition per the ticket's own guidance.

### Your directives

**Scope:** You conduct an adversarial review of the implementation diff and directly
related code paths named in the attack surfaces. Do not expand scope beyond what the
ticket outcome describes.

**Advisory-only — no file writes:** You must not create, modify, or delete any file in
the repository. Your entire deliverable is findings prose in the required output format
below. The primary execution agent owns all patches.

**Read boundary for delivery docs:** Do not write files under `docs/product/delivery/**`
(or anywhere else). You **must** still read the ticket Rationale and any referenced
contract docs as part of probing the "Doc-vs-code drift in the ticket Rationale"
diff-derived surface above. If you find drift — the Rationale claims a behavior the diff
does not implement, or the diff implements behavior the Rationale does not describe —
surface it under **Advisory Observations** with the specific file, the conflicting
claim, and what the diff actually does. The primary agent decides whether to patch docs
or code.

**Coverage mandate:** For each attack surface listed above, you must either probe it and
report what you found, or explain in one sentence why it does not apply. "I didn't check"
is not acceptable. A clean result on a surface you probed is a valid and valuable outcome.
Keep any added surfaces tied to the ticket behavior; do not turn this into broad style,
cleanup, or architecture review.

**Finding discipline:** Report a finding when one of the following holds:

1. The code breaks a stated invariant.
2. The code introduces a correctness gap you can demonstrate.
3. **Spec-permits-real-bug:** the ticket's stated contract literally permits the
   behavior, but that behavior is nevertheless unsafe in production (data loss,
   unrecoverable state, silent-failure exposure, security regression). Name which spec
   clause permitted the unsafe behavior so the primary agent can decide whether to update
   the spec.

Do not report style, preference, or hypothetical future requirements as blocking findings.
If you notice something worth flagging but it is outside these three clauses, put it in
**Advisory Observations** only.

**No fabrication pressure:** If all invariants hold and all attack surfaces are sound, your
correct output is a clean report. Do not invent findings to justify the review step.

---

### Required output format

After completing your review, report in this exact structure (prose only — no file edits).

- Use exactly these five top-level section headings, in this order:
  `Invariant results`, `Surface results`, `Actionable findings`,
  `Advisory Observations`, `Runner termination`.
- **Do not use `---` horizontal rules anywhere in the report.**
- **`Runner termination` must be the section heading**, not `**runnerStatus:**
  \`completed\`` or any other inline key-value variant.
- Inside `Advisory Observations`, write **one observation per bullet or one
  observation per paragraph**. Do NOT use a bold span (`**A1 — Title**`) on a
  line by itself before the observation body.

**Invariant results**
For each invariant: `[held | broken | untested]` — one line explaining what you tried.

**Surface results**
For each attack surface (both ticket-spec-derived and the seven diff-derived classes):
`[probed | N/A — <reason> | blocked — missing-input]`
If probed: what you tried and what you found (one to three sentences).

**Actionable findings**
For each finding the primary agent should consider patching: file/path, what is wrong,
which invariant or finding-discipline clause applies, and a concrete fix recommendation.
If none: "None."

**Advisory Observations**
Things you noticed that are outside the three finding-discipline clauses, including any
doc-vs-code drift surfaced under the diff-derived "Doc-vs-code drift in the ticket
Rationale" class. One bullet or one paragraph per observation. If none: "None."

**Runner termination**
`runnerStatus`: one of `completed | rate_limit | sandbox_denied | runner_unavailable`.
`terminatedReason`: one short sentence explaining why this status was reported.
